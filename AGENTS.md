## 项目概览

这是一个用于跟踪和预测《BanG Dream! Girls Band Party!》活动分数线的Telegram机器人。它提供了实时的分数线数据、预测功能以及相关的统计信息，帮助玩家更好地规划游戏策略。该项目使用Go语言编写，并利用了Telegram Bot API进行交互，具体的依赖项详见`go.mod`文件。

## 项目指令

- 在提交代码前，请确保运行`go fmt ./...`以格式化代码，运行`go vet ./...`以检查潜在的错误，最后必须保证`go test ./...`通过所有测试和`go build ./...`成功编译。
- 尽可能复用已有的函数和模块，避免重复造轮子。
- 编写新的函数时，请确保为其编写单元测试，并且在关键逻辑处添加必要的注释。
- 在使用终端之前，事先检查终端类型（Powershell 或 bash），确保其支持所需的功能。

## 注意事项

- 除非有明确的指令，否则可修改文件的范围仅限根目录及以下的文件夹，禁止修改根目录以外的文件。
- 严禁直接修改`config.yaml`文件中的配置，如新增可配置项，应修改根目录下的`example.yaml`文件。
- 严禁在代码中使用硬编码的API密钥或敏感信息，所有可配置的参数应通过配置文件进行管理。
